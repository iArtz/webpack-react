import React from 'react';
// import { hot } from 'react-hot-loader';

import './App.css';

const App = () => (
  <div>
    <h1>Webpack x React</h1>
  </div>
);

// export default hot(module)(App);
export default App
